Generate a badge from: $ARGUMENTS — pick a fitting emoji, a short badge name, and a one-sentence description. Then run:
`python3 ~/.claude/buddy-update.py badge "EMOJI" "BADGE_NAME" "DESCRIPTION"`
Print the output verbatim. Then add one short in-character line as Pokemon Master Coach.
