---
description: Challenge a gym leader to a PvP battle. Win earns the badge.
---

Usage:
- `/poke:battle` — list gym leaders + defeated status
- `/poke:battle <leader>` — challenge one: `brock`, `misty`, `surge`, `erika`, `koga`, `sabrina`, `blaine`, `giovanni`

Run `python3 "${CLAUDE_PLUGIN_ROOT}/buddy-update.py" battle $ARGUMENTS` and print the output verbatim.
