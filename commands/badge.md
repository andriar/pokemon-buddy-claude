---
description: Award a badge. Usage — /poke:badge <context of achievement>
---

Generate a badge from: $ARGUMENTS — pick a fitting emoji, a short badge name, and a one-sentence description. Then run:

`python3 "${CLAUDE_PLUGIN_ROOT}/buddy-update.py" badge "EMOJI" "BADGE_NAME" "DESCRIPTION"`

Print the output verbatim.
