---
description: Remove your trainer profile from Pokemon Buddy Hub
---

Run:

`python3 "${CLAUDE_PLUGIN_ROOT}/buddy-update.py" unpublish`

Print the output verbatim. This deletes the published snapshot from the hub. The user's local stats are untouched.
